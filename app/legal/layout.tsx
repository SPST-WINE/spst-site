import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "SPST - Termini, condizioni e privacy",
};

export default function LegalLayout({ children }: { children: React.ReactNode }) {
  return children;
}
